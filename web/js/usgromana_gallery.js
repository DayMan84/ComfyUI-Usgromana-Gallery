// ComfyUI-Usgromana-Gallery/web/js/usgromana_gallery.js

import { initGalleryExtension } from "../core/entry.js";

// The extension loader just imports this module.
// We immediately initialize the gallery when loaded.
initGalleryExtension();
